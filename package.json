{
  "name": "concept-book-base",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist --no-history --dotfiles"
  },
  "devDependencies": {
    "@rollup/plugin-yaml": "^5.0.0",
    "gh-pages": "^6.0.0",
    "playwright": "^1.60.0",
    "vite": "^5.4.0"
  }
}
